// Проверка премиум-подписки (пока заглушка, по образцу telegram.js).
//
// Сейчас возврат анкеты доступен всем — isPremium() всегда true.
// Когда подключим оплату, здесь появится настоящая проверка:
//   - поле в анкете пользователя, пришедшее с сервера, ИЛИ
//   - отдельный запрос к нашему backend.
// Весь остальной код уже спрашивает разрешение через эту функцию,
// поэтому включить платность можно будет правкой одного места.

export function isPremium() {
  return true;
}
